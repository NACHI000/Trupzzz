// How can you calculate the mean of a numeric column grouped by a factor column in R?
data <- data.frame( 
  group = c('A', 'A', 'B', 'B', 'c', 'c'), 
  score = c(85, 90, 78, 88, 92, 87) 
)
mean_scores <- tapply(data$score, data$group, mean) 
print(mean_scores)
aggregate_scores <- aggregate(score ~ group, data = data, FUN = mean) 
print(aggregate_scores)


// Write a program that categorises the numeric variable based on value ranges.
categorize_value <- function (num) {
  
  if (num >= 90) {
    return("Excellent")
  } else if (num >= 75) {
    return ("Good")
  } else if (num >= 50) {
    return ("Average")
  } else {
    return ("Poor")
  }
}
value <- 68
category <- categorize_value(value)
print(paste("The value", value, "is categorized as:", category))


// How can you calculate the square of each element in a vector?
nums <- c(1, 2, 3, 4, 5)
squares <- c()
for (i in nums) { 
  squares <- c(squares, i^2)
  }
print(squares)


// How can you create a loop that prints numbers from 1 to 5 using a while
count <- 1
while (count <= 5) {
  print(count)
  count <- count + 1
}


// How can you return different outputs based on the value of a variable using switch?
option <- "B"
result <- switch (option,
                  "A" = "You chose option A",
                  "B" = "You chose option B",
                  "C" = "You chose option C",
                  "Invalid option"
)
print(result)


// Write a program that checks whether each number in a vector is even or odd.
numbers <- c(1, 2, 3, 4, 5, 6)
result <- ifelse (numbers %% 2 == 0, "Even", "Odd") 
print(result)
